Сервер:

Не делать блоки из торпед(не считать такие блоки возможными)
1ЛН > 2 ЭС ? (спрашивал КР одним ЭС, второй стоял рядом)
1ЛН > 2 КР ? (то же самое)

Game::ask
там где switch(result) расставить order, state(для клиентов и сервера) для каждого случая

Доделать Game::ans
